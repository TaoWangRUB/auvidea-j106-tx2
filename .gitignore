# Large archive/tar files
*.tar.gz
*.tar.bz2
*.tbz2
*.tgz
*.zip
*.7z
*.rar

# Extracted BSP and SDK directories (large, can be re-extracted from public sources)
bsp_L4T_24_2_1_V2.2/
J90_v2.2_4.2.2/

# Build tree: L4T sources, toolchain, kernel build output, DTBs (multi-GB, reproducible)
j106build/

# Binary/image files
*.img
*.bin
*.raw
*.dtb
Image

# Claude settings
.claude/

# R32.2.1 BSP comparison (large, reproducible from public sources)
bsp-r32.2/official/
bsp-r32.2/auvidea/
bsp-r32.2/*.dts
bsp-r32.2/*.log
__pycache__/
hw-trigger/firmware/build/
hw-trigger/firmware-f401/build/

# ...but the firmware rollback image is deliberately tracked: build/ is ignored,
# so this is the only copy of the pre-restructure trigger binary.
!hw-trigger/firmware/known-good/*.bin
